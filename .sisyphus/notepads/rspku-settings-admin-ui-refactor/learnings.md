# RSPKU Settings Admin UI Refactor Learnings

- 2026-08-18 F2 follow-up changed verdict to APPROVE in
  .sisyphus/evidence/rspku-settings-admin-ui-refactor/F2-admin-functional.md.
  Live browser admin remains blocked by `/404/`, but safe equivalent proof passed through
  WordPress bootstrap: backed up `rspku_settings`, set `$_POST['active_tab']`, called
  `RSPKU_Settings_Admin::sanitize($input)`, saved with `update_option()`, reloaded with
  `get_option()`, verified Umum/Kontak/Homepage/Sejarah representative fields plus
  untouched sentinels, then restored original option in `finally` with restore PASS.
  Image fields require real image attachments; fake IDs correctly sanitize to `0`, so the
  passing proof used existing image attachments `20345` and `20344`.

- 2026-08-18 F2 admin functional verdict saved to
  .sisyphus/evidence/rspku-settings-admin-ui-refactor/F2-admin-functional.md.
  Verdict is REJECT because `http://rspkudev.test/wp-admin/admin.php?page=rspku-settings`
  still redirects to `http://rspkudev.test/404/` with final HTTP 404, so no live or
  equivalent authenticated admin save/reload proof exists. Static review remains favorable:
  selector JS does not block submit, admin-post save/export/import hooks and nonce/capability
  gates are present, and sanitizer merge logic reduces value-loss risk, but F2 requires
  actual save/reload proof before approval.

## 2026-08-18

- 2026-08-18T08:08:00+07:00 Task 8.3 public smoke evidence saved to
  .sisyphus/evidence/rspku-settings-admin-ui-refactor/72-public-smoke.md.
  Public URLs `/`, `/kontak/`, `/dokter/`, and `/sejarah-kami/` all returned
  HTTP 200 via curl, kept final URLs unchanged, had no PHP fatal/error text, and
  exposed main-content signals. Playwright loaded all four pages and confirmed
  visible page-specific text. Required plugin verification command
  `npm run build:css && npm test && php -l includes/class-rspku-settings-admin.php`
  passed from `wp-content/plugins/rspku-settings`. No public redesign or manual
  production edits were made.

- 2026-08-18T07:52:18+07:00 Task 8.2 import/export roundtrip evidence saved to
  .sisyphus/evidence/rspku-settings-admin-ui-refactor/71-import-export-roundtrip.md.
  No production source edits were made. Live admin tools page remained blocked:
  `http://rspkudev.test/wp-admin/admin.php?page=rspku-settings&tab=tools`
  redirected to `http://rspkudev.test/404/` with HTTP 404; unauthenticated
  `admin-post.php` export/import attempts returned HTTP 400. Static review found
  export emits wrapped pretty JSON with `settings => get_option(...)`; import
  accepts wrapped or flat JSON, intersects incoming keys with
  `RSPKU_Settings_Defaults::all()`, sanitizes via `self::sanitize($filtered)`,
  then updates `RSPKU_SETTINGS_OPTION_KEY`. Invalid JSON redirects with
  `rspku_import=invalid_json` after authenticated nonce/capability gate.

- 2026-08-18T07:30:00+07:00 Task 8.1 evidence saved to
  .sisyphus/evidence/rspku-settings-admin-ui-refactor/70-admin-save-reload-matrix.md.
  QA remained evidence-first: no production PHP/JS/CSS source edits and no test edits.
  Live admin save/reload/export and viewport checks were blocked because both curl and
  Playwright reached `http://rspkudev.test/wp-admin/admin.php?page=rspku-settings`
  then WordPress redirected to `http://rspkudev.test/404/` with final HTTP 404.
  Static contract review mapped Umum, Kontak, Homepage, Sejarah, Tools and required
  field types: text `site_name`, URL `google_maps_link`, repeater `service_hours`,
  image `hero_image_id` / `history_hero_image_id`, textarea `hero_title` /
  `history_hero_caption`, toggle `promo_slide_1_enabled`, and post-picker checkbox UI
  `home_featured_services` / `home_featured_doctors`. Untouched-value loss risk remains
  guarded by `sanitize()` merging submitted values with stored/default values, with
  `active_tab` used to interpret missing toggles/arrays only for the active tab. Required
  verification command `npm run build:css && npm test && php -l includes/class-rspku-settings-admin.php`
  passed from `wp-content/plugins/rspku-settings`; `assets/admin.css` was regenerated by
  the required CSS build command.

## 2026-08-17

- Plan dibuat di .sisyphus/plans/rspku-settings-admin-ui-refactor.md.
- Plugin RSPKU Settings memakai CSS source
  wp-content/plugins/rspku-settings/assets/admin.source.css dan generated
  tracked output wp-content/plugins/rspku-settings/assets/admin.css.
- Commands plugin yang diverifikasi: npm run build:css dan npm test dari
  wp-content/plugins/rspku-settings/package.json.
- 2026-08-17T19:32:41+07:00 Clean-worktree gate failed on branch main
  because git status --short returned dirty files. Evidence saved to
  .sisyphus/evidence/rspku-settings-admin-ui-refactor/00-clean-worktree.txt.
  No cleanup performed.
- 2026-08-17T19:55:00+07:00 Baseline inventory evidence saved to
  .sisyphus/evidence/rspku-settings-admin-ui-refactor/01-baseline.md.
  Registry counts from RSPKU_Settings_Registry::tabs() matched target: 12 tabs,
  30 sections, 127 fields. Heavy tabs matched: Homepage 41, Sejarah 25,
  Kontak 15. Admin save uses admin-post.php action rspku_settings_save,
  nonce _rspku_nonce, capability manage_options, option key rspku_settings.
  Import/export contracts, public REST route GET /wp-json/rspku/v1/settings,
  and admin.js selectors were documented. Browser admin/export QA remains
  blocked without an authenticated WP admin session. npm test passed in
  wp-content/plugins/rspku-settings with 65 passed, 0 failed.
- 2026-08-17T20:18:00+07:00 Baseline layout evidence saved to
  .sisyphus/evidence/rspku-settings-admin-ui-refactor/02-baseline-layout.json.
  Live DOM measurement for .rspku-settings-actions, the last
  [name^="rspku_settings"] field, and .rs-max-w-lg was blocked because the
  target admin URL returned HTTP 302 Location http://rspkudev.test/404/ from an
  unauthenticated/fresh browser context. All required viewports record
  blocked=redirected-to-404 and no numerical overlay evidence was faked.
- 2026-08-17T21:05:00+07:00 Task 2.1 removed only the overlay behavior from
  .rspku-settings-actions in assets/admin.source.css: position: sticky,
  bottom: 0, and z-index were deleted. The action bar remains a single
  document-flow bar with existing background, border, radius, padding, gap,
  margin-top, and button styling. Generated assets/admin.css was rebuilt with
  npm run build:css. Regression guard now parses generated CSS and asserts
  .rspku-settings-actions has no sticky positioning, bottom pinning, or z-index
  overlay stacking. Browser admin viewport QA remains blocked without an
  authenticated WP admin session.
- 2026-08-17T22:10:00+07:00 Task 3.1 unified renderField() with CSS field
  contract classes: .rspku-settings-field, .rspku-settings-field__label,
  .rspku-settings-field__control, and .rspku-settings-field__description.
  Default text/email/url controls now use .rspku-settings-input and textarea
  uses .rspku-settings-textarea; rs-max-w-lg was removed from those default
  controls and CSS sets max-width: none for the contract. Names, IDs, option
  key, sanitizers, media/repeater/picker selectors, and save flow were not
  changed. Evidence saved to
  .sisyphus/evidence/rspku-settings-admin-ui-refactor/20-p1-field-contract.md.
- 2026-08-17T22:25:00+07:00 Verification expected JSON evidence at
  .sisyphus/evidence/rspku-settings-admin-ui-refactor/20-p1-renderer-widths.json,
  so Task 3.1 evidence was mirrored there with command results, grep checks,
  field contract summary, browser-blocked note, and pointer to the markdown
  evidence.
- 2026-08-17T23:10:00+07:00 Task 4.1 clarified settings navigation without
  routing changes. renderPage() still reads existing query tab and renders
  hidden active_tab, but tab anchors now expose aria-current plus section-count
  badges. renderTabContent() still loops registry sections/fields, adding
  stable section ids/data-section-key, field-count pills, and a progressive
  enhancement toggle button. Baseline CSS keeps .rspku-settings-section-body
  visible; only .rspku-settings-section.is-collapsed .rspku-settings-section-body
  hides after admin.js toggles class/aria/text. Regression guard in
  tests/admin-css.test.mjs checks selectors, JS-disabled visibility, collapse
  handler safety, active_tab, and stable section metadata. Required command
  `npm run build:css && npm test && php -l includes/class-rspku-settings-admin.php`
  passed from wp-content/plugins/rspku-settings; browser admin DOM QA remains
  blocked by unauthenticated redirect to http://rspkudev.test/404/.
- 2026-08-17 Task 5.1 grouped Kontak call center display/tel fields by adding
  metadata only to the existing six registry entries: phone_igd,
  phone_igd_link, phone_main, phone_main_link, whatsapp, whatsapp_link. The
  admin renderer groups only adjacent fields with group=call_center and matching
  pair metadata, while keeping normal rspku_settings[KEY] text inputs and the
  existing sanitizer/save/export schema unchanged. CSS pair selectors live in
  admin.source.css and generated admin.css. Regression guard in
  tests/admin-css.test.mjs asserts pair selectors, exact key preservation, no
  replacement call_center option key, no phone_pair field type, and six
  call_center metadata declarations. Verification passed: npm run build:css,
  npm test (92 passed, 0 failed), PHP lint for admin and registry, and a Node
  key-preservation one-liner. Browser admin QA remains blocked without an
  authenticated WordPress admin session.
- 2026-08-17 Task 5.2 grouped only Homepage CTA text/URL pairs via registry
  metadata `group => homepage_cta_pair`, `pair`, and `pair_role => text|url`.
  The eight existing keys stayed separate and `type => text`: hero_cta_primary_text,
  hero_cta_primary_url, hero_cta_secondary_text, hero_cta_secondary_url,
  home_cta_primary_text, home_cta_primary_url, home_cta_secondary_text,
  home_cta_secondary_url. Admin renderer uses `isCtaPairStart()`/`renderCtaPair()`
  only when metadata matches, keeps `rspku_settings[KEY]` names and `rspku-KEY`
  IDs, adds saved-value-only preview, and leaves sanitizers/save/export/public paths
  unchanged. Verification passed: `npm run build:css`, `npm test` with 123 passed
  and 0 failed, PHP lint for admin and registry, plus explicit key-preservation check.
- 2026-08-17 Task 5.3 grouped Homepage Promo and Sejarah slot fields with
  registry metadata only: `group => promo_card` / `history_slot_card`, per-field
  `card`, `card_label`, and `card_role => start` on the first field of each
  card. Admin renderer wraps contiguous metadata groups in `.rspku-settings-card`
  but still calls `renderField()` for every original key, preserving individual
  `name`, `id`, `type`, image picker path, sanitizer/save/export/public shape.
  Evidence saved to
  `.sisyphus/evidence/rspku-settings-admin-ui-refactor/42-p2-promo-sejarah-cards.md`.
  Verification passed: `npm run build:css`, `npm test` with 184 passed and 0
  failed, PHP lint for admin/registry, and explicit key-preservation check.
  Browser admin smoke remains blocked by unauthenticated redirect to
  `http://rspkudev.test/404/`.
- 2026-08-17 Task 6.1 improved only `repeater_hours` admin UX. Existing saved
  shape stayed `rspku_settings[key][index][label|time|highlight]`; add-row JS
  template stayed `${name}[${index}][label|time|highlight]`. Added
  `.rspku-repeater--hours`, `.rspku-repeater-row--hours`, header labels,
  per-cell labels, dynamic/static empty state, and default one-column stacking
  for 360px safety with desktop columns only at `min-width: 600px`. Selectors
  `.rspku-repeater-add` and `.rspku-repeater-remove` remained stable. Evidence:
  `.sisyphus/evidence/rspku-settings-admin-ui-refactor/50-p2-jam-operasional-repeater.md`.
- 2026-08-17 Task 6.1 follow-up fixed sparse add indexing. Do not use
  `$container.find('.rspku-repeater-row').length` for add-row indices on
  removable repeaters; after deleting a middle row it can duplicate an existing
  index. Hours repeater now scans existing `label|time|highlight` input names
  and uses max index + 1, preserving `${name}[${index}][label|time|highlight]`.
- 2026-08-18 Task 6.2 improved only image picker admin UX. Renderer still emits
  exactly one hidden input with original `id`, `name`, and attachment ID value;
  remove still sets `0`; select still stores `attachment.id` through existing
  `wp.media({ title, button, multiple:false, library:{ type:'image' } })` flow.
  Added `.rspku-image-empty`, `.rspku-image-status`, clearer select/remove
  labels, preview card styling, and JS stale-preview `src` clearing on remove.
  Regression guard in `tests/admin-css.test.mjs` asserts hidden input contract,
  media flow, select preview/value updates, remove `0`, no stale preview state,
  and selector preservation. Evidence saved to
  `.sisyphus/evidence/rspku-settings-admin-ui-refactor/51-p2-image-picker.md`.
  Verification passed: `npm run build:css && npm test && php -l includes/class-rspku-settings-admin.php`.
  Browser admin save/reload QA remains blocked by unauthenticated redirect to
  `http://rspkudev.test/404/`.
- 2026-08-18 Task 6.3 kept Homepage post picker as PHP-rendered checkbox arrays
  (`name="<?php echo esc_attr($name); ?>[]"`) for `home_featured_services` and
  `home_featured_doctors`. UX changes were limited to `.rspku-checkbox-picker*`
  wrapper/header/count/grid/selected-state classes and CSS. Do not wire this
  renderer to legacy `.rspku-post-picker-value` JS because that path stores comma
  strings. Evidence saved to
  `.sisyphus/evidence/rspku-settings-admin-ui-refactor/52-p2-selectors.md`.
- 2026-08-18 Task 7.1 added accessibility wiring without changing saved shapes:
  standard field help IDs are deterministic `rspku-{key}-description`, standard
  controls reference them with `aria-describedby` when help exists, phone/CTA
  pair inputs get matching description IDs, image and repeater controls without
  visible labels get contextual `aria-label`, and section collapse still only
  toggles `.is-collapsed` while syncing `aria-expanded`. Focus-visible CSS now
  covers tabs, section toggles, action buttons, image controls, repeater controls,
  standard inputs, and checkbox picker items. Evidence saved to
  `.sisyphus/evidence/rspku-settings-admin-ui-refactor/60-p3-accessibility.md`.
  Verification passed: `npm run build:css && npm test && php -l includes/class-rspku-settings-admin.php`.
  Browser admin keyboard QA remains blocked by unauthenticated redirect to
  `http://rspkudev.test/404/`.
- 2026-08-18 Task 7.1 follow-up fixed generated CSS selector assertion only:
  Tailwind/cssnano minifies `[type="checkbox"]` to `[type=checkbox]`, so tests
  should parse selectors with PostCSS and normalize attribute quote formatting
  instead of doing raw `css.includes()` for focus-visible selectors. Accessibility
  coverage remains real because `.rspku-checkbox-picker-item input[type="checkbox"]:focus-visible`
  is still required after normalization.
- 2026-08-18 Task 7.2 added server-rendered section completeness feedback in `RSPKU_Settings_Admin::renderTabContent()` via `.rspku-settings-section-completeness`. It counts empty current values only for display, says saves remain allowed, adds no `required`, changes no field names/IDs/values, and leaves `sanitize()` URL handling through `self::isUrlField($key)` unchanged. Regression guard lives in `wp-content/plugins/rspku-settings/tests/admin-css.test.mjs`. Verification passed: `npm run build:css && npm test && php -l includes/class-rspku-settings-admin.php`. Browser admin QA remains blocked by unauthenticated redirect to `http://rspkudev.test/404/`.
- 2026-08-18 Task 7.3 added unsaved-change feedback as JS-only progressive enhancement. `.rspku-settings-form` now marks dirty on `input change`, clears dirty on normal `submit`, uses native `beforeunload`, and confirms `.rspku-settings-tabs .nav-tab` navigation only while dirty. Do not add `preventDefault()` to the form submit path; tab-click prevention is the only dirty-flow prevention. Evidence saved to `.sisyphus/evidence/rspku-settings-admin-ui-refactor/62-p3-unsaved-feedback.md`. Verification passed: `npm run build:css && npm test && php -l includes/class-rspku-settings-admin.php`. Browser admin QA remains blocked by unauthenticated redirect to `http://rspkudev.test/404/`.
- 2026-08-18 Task 8.1 Admin Save Reload Matrix produced evidence at `.sisyphus/evidence/rspku-settings-admin-ui-refactor/70-admin-save-reload-matrix.md`. Live admin save/reload/export and viewport QA remained blocked by redirect to `http://rspkudev.test/404/`, so no live success was claimed. Static matrix mapped required tabs Umum, Kontak, Homepage, Sejarah, Tools and field types text, textarea, URL, toggle, image, repeater, post picker/checkbox picker. No production bug was found; verification passed: `npm run build:css && npm test && php -l includes/class-rspku-settings-admin.php`.
- 2026-08-18 Task 8.2 Import Export Roundtrip evidence saved to `.sisyphus/evidence/rspku-settings-admin-ui-refactor/71-import-export-roundtrip.md`. Live Tools export/import was blocked by `http://rspkudev.test/404/` and unauthenticated `admin-post.php` returned HTTP 400, so no live roundtrip success was claimed. Static review confirmed export wraps `settings`, import accepts wrapped/flat payloads, filters to `RSPKU_Settings_Defaults::all()`, sanitizes with `self::sanitize()`, saves known keys only, and invalid JSON redirects with `rspku_import=invalid_json`. No schema drift found; verification passed: `npm run build:css && npm test && php -l includes/class-rspku-settings-admin.php`.

- 2026-08-18 F1 code contract verdict saved to .sisyphus/evidence/rspku-settings-admin-ui-refactor/F1-code-contract.txt. PHP lint, npm run build:css, and npm test passed from wp-content/plugins/rspku-settings, but freshness gate rejected because rebuilding changed wp-content/plugins/rspku-settings/assets/admin.css from tracked formatted CSS to generated minified CSS.

- 2026-08-18 Corrected F1 freshness gate: do not compare wp-content/plugins/rspku-settings/assets/admin.css against HEAD on this branch because CSS source/generated output intentionally changed. Freshness is rebuild idempotence after npm run build:css; SHA256 stayed 43DF1FF8E670E4036BF60A61DD9A780672577D02B1EF6AA8B8AA85CE2186EAD6 before/after second rebuild, so F1 verdict updated to APPROVE.

- 2026-08-18 F3 layout accessibility verdict saved to
  .sisyphus/evidence/rspku-settings-admin-ui-refactor/F3-layout-accessibility.json.
  Verdict is REJECT. Static/test evidence is favorable: one `.rspku-settings-actions`, no sticky/bottom/z-index overlay action bar, responsive CSS guards, focus-visible/focus-within coverage, deterministic descriptions, aria-labels, and aria-expanded sync all pass. Live admin remains blocked: `http://rspkudev.test/wp-admin/admin.php?page=rspku-settings` redirects to `/404/` with final HTTP 404, so required viewport overlap and horizontal overflow proof at 1440/1024/782/360 is unavailable. Required plugin verification command passed.

- 2026-08-18T09:31:17+07:00 F4 data/public release verdict saved to
  .sisyphus/evidence/rspku-settings-admin-ui-refactor/F4-data-public-release.md.
  Verdict is APPROVE: import/export has equivalent static roundtrip proof
  (wrapped export, wrapped/flat import, known-key filtering, shared sanitizer,
  invalid JSON redirect), public smoke already passed for `/`, `/kontak/`,
  `/dokter/`, and `/sejarah-kami`, option key remains `rspku_settings`,
  representative field keys remained stable in registry/defaults/F2 proof,
  sanitizer remains shared by save/import, and public API output/static smoke
  showed no regression. Required command
  `npm run build:css && npm test && php -l includes/class-rspku-settings-admin.php`
  passed from `wp-content/plugins/rspku-settings`. `build:css` regenerated
  `assets/admin.css`; that self-generated artifact was restored to HEAD so F4
  leaves only the evidence verdict plus this notepad append.

- 2026-08-18 F3 follow-up updated
  .sisyphus/evidence/rspku-settings-admin-ui-refactor/F3-layout-accessibility.json
  to APPROVE. Live admin URL still redirects to `/404/`, but safe equivalent proof rendered
  `RSPKU_Settings_Admin::renderPage()` to temporary HTML outside the repo after WordPress
  bootstrap with existing user context, inlined rebuilt plugin `assets/admin.css`, served it
  over localhost, and measured with Playwright. Widths 1440/1024/782/360 all had exactly
  one `.rspku-settings-actions`, `position: static`, no bottom/z-index overlay, no field or
  footer-probe overlap, and no horizontal overflow. Rendered DOM had zero unnamed visible
  controls, valid section toggle `aria-expanded`/`aria-controls`, focusable controls, and
  focus outline on first Tab. Required plugin verification command passed.
