Task 6 - Harden Public RSPKU Core REST Endpoints
Date: 2026-07-31

Changed file:
- wp-content/plugins/rspku-core/rspku-core.php

Code changes verified:
- Added SEARCH_PER_PAGE_MAX = 50 and COLLECTION_PER_PAGE_MAX = 50.
- /search per_page clamps to SEARCH_PER_PAGE_MAX.
- Collection routes now call enforce_rate_limit('collection_' . $postType, 120, 60).
- Collection per_page clamps to COLLECTION_PER_PAGE_MAX.
- Existing Task 4 client_ip policy preserved: REMOTE_ADDR default, forwarded headers trusted only when REMOTE_ADDR is configured trusted proxy.
- enforce_rate_limit returns HTTP 429 with Retry-After, X-RateLimit-Limit, and X-RateLimit-Remaining headers.

Command: php -l wp-content/plugins/rspku-core/rspku-core.php
Output:
No syntax errors detected in wp-content/plugins/rspku-core/rspku-core.php

LSP diagnostics:
No diagnostics found for wp-content/plugins/rspku-core/rspku-core.php

Static grep checks:
- SEARCH_PER_PAGE_MAX and COLLECTION_PER_PAGE_MAX present.
- /search uses enforce_rate_limit('search', 60, 60).
- get_collection uses enforce_rate_limit('collection_' . $postType, 120, 60).
- Retry-After header exists in enforce_rate_limit.
- HTTP_X_FORWARDED_FOR only appears inside gated client_ip trusted-proxy branch.
- No TODO, FIXME, HACK, var_dump, print_r, or error_log debugging additions found in rspku-core plugin.

HTTP QA: public endpoint /site
Command:
curl.exe -s -o NUL -w "%{http_code} %{time_total} %{size_download}" "http://rspkudev.test/wp-json/rspku/v1/site"
Observed:
200 3.254365 2182

HTTP QA: public endpoint /home
Command:
curl.exe -s -o NUL -w "%{http_code} %{time_total} %{size_download}" "http://rspkudev.test/wp-json/rspku/v1/home"
Observed:
200 2.548691 84364

HTTP QA: search per_page clamp
Command:
$j = Invoke-RestMethod -Uri "http://rspkudev.test/wp-json/rspku/v1/search?q=rs&per_page=999"; "search_per_page=$($j.per_page) search_items=$($j.items.Count)"
Observed:
search_per_page=50 search_items=50

HTTP QA: collection per_page clamp
Command:
$j = Invoke-RestMethod -Uri "http://rspkudev.test/wp-json/rspku/v1/posts?per_page=999"; "posts_items=$($j.Count)"
Observed:
posts_items=50

HTTP QA: search rate limit + spoofed X-Forwarded-For ignored
Setup:
php -r "require 'wp-load.php'; foreach (array('127.0.0.1','::1','0.0.0.0') as `$ip) { set_transient('rspku_rl_search_' . md5(`$ip), 60, 60); }"
Request:
curl.exe -i -s "http://rspkudev.test/wp-json/rspku/v1/search?q=rs&per_page=1" -H "X-Forwarded-For: 203.0.113.99"
Observed:
HTTP/1.1 429 Too Many Requests
Retry-After: 60
X-RateLimit-Limit: 60
X-RateLimit-Remaining: 0
{"code":"rspku_rate_limited","message":"Too many requests. Please slow down.","data":{"status":429}}
Cleanup:
php -r "require 'wp-load.php'; foreach (array('127.0.0.1','::1','0.0.0.0') as `$ip) { delete_transient('rspku_rl_search_' . md5(`$ip)); }"

HTTP QA: collection rate limit + spoofed X-Forwarded-For ignored
Setup:
php -r "require 'wp-load.php'; foreach (array('127.0.0.1','::1','0.0.0.0') as `$ip) { set_transient('rspku_rl_collection_post_' . md5(`$ip), 120, 60); }"
Request:
curl.exe -i -s "http://rspkudev.test/wp-json/rspku/v1/posts?per_page=1" -H "X-Forwarded-For: 203.0.113.100"
Observed:
HTTP/1.1 429 Too Many Requests
Retry-After: 60
X-RateLimit-Limit: 120
X-RateLimit-Remaining: 0
{"code":"rspku_rate_limited","message":"Too many requests. Please slow down.","data":{"status":429}}
Cleanup:
php -r "require 'wp-load.php'; foreach (array('127.0.0.1','::1','0.0.0.0') as `$ip) { delete_transient('rspku_rl_collection_post_' . md5(`$ip)); }"

Field exposure review:
- Normal public responses still expose expected published-content fields: id, type, slug, title, url/path, date/modified, excerpt, images, categories/tags, author public name/slug, SEO title/description/canonical, and type-specific public fields.
- No new response fields were added by this task.
- ACF exposure remains controlled by existing plugin filter gates; this task did not add private/admin field exposure.
